 // Multiple Animation


 function doSwitch(i, j, m, l, rev, animate, reverseFunc) { //Nested Functions for switching of buttons animation

     function runFrames(A, B,horizontal) 
         function movement () 
 }

 function hideButtons (A, B) { //Hiding buttons 
 }

 function switchColors (A,B) { //Switching buttons colors
 }

 function switchNumber (i,j,m,l) { //Switching button numbers
 }

 function getButtons(a, b, c, d) { //Getting buttons
 }


 function moveRL(buttonA, buttonB, delta) { //Define both buttons horizontal movements with delta location
 }

 function moveUpDown(buttonA, buttonB, delta) { //Define both buttons vertical movements with delta location
 }

 var deltaHelper = { //Help with Delta data to move start,end, how much each movement etc.
 };

 function runTimer () { //Run heading timer animation.
 }

 function stopTimer () { //Stop heading timer.
 }
